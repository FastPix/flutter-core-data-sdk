name: fastpix_flutter_core_data_example
description: "Example app demonstrating fastpix_flutter_core_data SDK usage on Android and iOS."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  video_player: ^2.9.1
  fastpix_flutter_core_data:
    path: ../
  better_player_plus: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
